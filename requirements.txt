# ── PACKAGE INTEGRITY: LICENSE ISSUES ────────────────────────────────────────
# GPL-3.0 — copyleft, infectious license, risky for commercial products
pyGPLsample==1.0.0

# AGPL-3.0 — strongest copyleft, requires SaaS source disclosure
# Real packages with AGPL:
mongo-python-driver==3.12.0

# GPL-2.0
# Real package with GPL-2.0 license:
sarge==0.1.7

# LGPL-2.1
uuid==1.30

# SSPL — not OSI approved, used by MongoDB
# Flagged as non-permissive commercial risk

# ── PACKAGE INTEGRITY: OPERATIONAL RISKS ─────────────────────────────────────

# Abandoned / unmaintained — last release 5+ years ago
pycrypto==2.6.1

# Deprecated — maintainer explicitly deprecated this package
distribute==0.7.3

# Single maintainer / bus factor 1 risk
easily==0.2.1

# Very low download count / obscure package
unipath==1.1

# No source repository listed
antiorm==1.2.1

# Unmaintained and known vulnerable
BeautifulSoup==3.2.2

# Deprecated in favor of another package
wsgiref==0.1.2

# Old, unmaintained XML library
pyxml==0.8.4

# ── SCA: KNOWN CVEs ───────────────────────────────────────────────────────────
Flask==0.12.4
Jinja2==2.10
requests==2.18.4
PyYAML==5.3.1
cryptography==2.8
urllib3==1.24.1
paramiko==2.4.1
Pillow==8.3.2
lxml==4.6.2
celery==4.3.0
SQLAlchemy==1.3.0

# ── STANDARD DEPS ─────────────────────────────────────────────────────────────
boto3==1.9.0
psycopg2-binary==2.8.6
redis==3.5.3
gunicorn==19.9.0
